'use server'

export async function forceClockOutAdmin(userId: string) {
    console.log(`[STUB] Forcing clock out for user ${userId}`);
    return { success: false, message: "Función administrativa forceClockOutAdmin no implementada aún." };
}
